
if(BSJ_AI_PLATFORM_ROCKCHIP)
    file(GLOB ROCKCHIP_SRC ${CMAKE_CURRENT_LIST_DIR}/rknn/*)
    file(GLOB ROCKCHIP_SRC ${ROCKCHIP_SRC} ${CMAKE_CURRENT_LIST_DIR}/ImageManagerRga/*.h)
    list(APPEND BSJ_AI_SRC ${ROCKCHIP_SRC})
elseif(BSJ_AI_PLATFORM_SIGMASTAR)   

endif()
